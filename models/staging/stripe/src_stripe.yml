version: 2

sources:
  - name: stripe
    database: raw
    schema: stripe
    tables:
    # 1 table
      - name: payment
        columns:
          - name: id
            tests:
              - unique              
              - not_null
        # source freshness
        loaded_at_field: _batched_at
        freshness:
          warn_after: {count: 12, period: hour}
          error_after: {count: 24, period: hour}